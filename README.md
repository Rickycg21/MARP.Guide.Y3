# MARP.Guide.Y3

## Tier 1 Choice:
Basic Monitoring Dashboard

## Tier 2 Choice:
Hybrid Search

Combined discovery + fetch

docker compose up --build rabbitmq ingestion

Run in BROWSER:
    http://localhost:5001/docs

1. Discover all PDFs and fetch them at once:
    curl.exe -X POST http://localhost:5001/documents/discover_and_fetch


2. Then list documents with all details:
    curl.exe http://localhost:5001/documents
